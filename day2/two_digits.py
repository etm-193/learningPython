#Two digit challenge

# Putting casting data types and previous concepts into practice. Fun project for later automate
# this program with a loop and insert an exception to prevent the user from not imputting any data.

two_digit_number = input("Type a two digit number: ")

a = int(two_digit_number[0])
b = int(two_digit_number[1])

result = a + b
print (result)
